# Claude Code for Murderbox

## Install

```bash
pikaur -S aur/claude-code # NOT ACTUALLY sure this needs to be run
```

- run `src/install.sh` once

## Updates

- run `claude-docker --rebuild` after making local changes to src etc.
- run `claude-docker --rebuild --no-cache` to pull in claude and possibly OS changes
